# 需求变更记录

本文档只记录已确认的需求变更及影响。具体执行契约仍以 active 任务书为准，
设计原因以 ADR 为准。

## 2026-08-16

### 建立需求文档四层结构

- 将当前实体抽取任务书登记为 `REQ-002`；
- 将已完成的核心原型和文档预处理需求移入 `archive/`；
- 将图谱分层和模型流水线拆分为 ADR；
- 新增 `manifest.yaml` 作为机器可读索引。

影响：后续智能体仅执行 manifest 中同时标记为 active 和 approved 的需求；
聊天记录不再作为需求依据。
